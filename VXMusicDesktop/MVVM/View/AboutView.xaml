<UserControl x:Class="VXMusicDesktop.MVVM.View.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VXMusicDesktop.MVVM.View"
             xmlns:custom="clr-namespace:VXMusicDesktop.Theme"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent">

    <UserControl.Resources>

        <SolidColorBrush x:Key="PrimaryColor" Color="{Binding Source={x:Static custom:ColourSchemeManager.PrimaryColour}, Path=Color}" />
        <SolidColorBrush x:Key="SecondaryColor" Color="{Binding Source={x:Static custom:ColourSchemeManager.SecondaryColour}, Path=Color}" />
        <SolidColorBrush x:Key="Accent1Colour" Color="{Binding Source={x:Static custom:ColourSchemeManager.Accent1Colour}, Path=Color}" />
        <SolidColorBrush x:Key="Accent2Colour" Color="{Binding Source={x:Static custom:ColourSchemeManager.Accent2Colour}, Path=Color}" />
        <SolidColorBrush x:Key="TextBasic" Color="{Binding Source={x:Static custom:ColourSchemeManager.TextBasic}, Path=Color}" />

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- First TextBlock -->
            <RowDefinition Height="Auto"/>
            <!-- Second TextBlock -->
        </Grid.RowDefinitions>

        <TextBlock
            Name="AboutTextHeader"
            Grid.Row="0"
            VerticalAlignment="Center"
            TextAlignment="Center"
            FontFamily="/Fonts/#Brushstrike"
            Foreground="{StaticResource TextBasic}"
            FontSize="80">
            VXMusic
        </TextBlock>

        <TextBlock
            Name="AboutTextParagraph"
            Grid.Row="1"
            VerticalAlignment="Center"
            TextAlignment="Center"
            Foreground="{StaticResource TextBasic}"
            FontSize="18">
            <Hyperlink NavigateUri="https://github.com/Soapwood" Click="Url_Click" TargetName="_blank">
                <Run Text="Github"/>
            </Hyperlink>
            <LineBreak/>
            <Hyperlink NavigateUri="https://discord.gg/acfpfHB7tU" Click="Url_Click" TargetName="_blank">
                <Run Text="Discord"/>
            </Hyperlink>
            <LineBreak/>
            <Hyperlink NavigateUri="https://twitter.com/soapwood_" Click="Url_Click" TargetName="_blank">
                <Run Text="Twitter / X"/>
            </Hyperlink>
            <LineBreak/><LineBreak/>
            VXMusicDesktop © Soapwood 2024
            <LineBreak/><LineBreak/>
            VXMusic Open Source Library coming Soon™
            <LineBreak/><LineBreak/>
            BRUSHSTRIKE is a brush typeface designed by Francesco Canovaro
        </TextBlock>
    </Grid>
</UserControl>
